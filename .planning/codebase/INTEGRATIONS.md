# Integrations & External Services
- **Database Provider:** Local SQLite file (`enjaz.db`).
- **Frontend-Backend:** The frontend (React Vite dev server/static build) communicates via CORS with the ASP.NET Core REST API backend running locally.
- **Tools:** Node.JS/npm tools for frontend build, `dotnet` CLI for backend build and EF core toolset.
